import React from "react";

const Locales = () => {
  return (
    <div>
      Ru En
      {/* <button onClick={() => setCurrentLocale(LOCALES.RUSSIAN)}>Русский</button>
      <button onClick={() => setCurrentLocale(LOCALES.ENGLISH)}>English</button> */}
    </div>
  );
};

export default Locales;
